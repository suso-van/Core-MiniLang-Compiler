# Core-MiniLang-Compiler
MiniLang Compiler is a custom compiler and stack-based virtual machine for a simple programming language. It implements the full compilation pipeline including lexical analysis, parsing, AST generation, bytecode compilation, and deterministic runtime execution, demonstrating core concepts of compiler design and language runtimes.
